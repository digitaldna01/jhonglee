# Copy to .env (gitignored) next to the compose file you run.
#   ANTHROPIC_API_KEY  optional — without it chat answers are retrieval-only
#   POSTGRES_PASSWORD  required on the Pi; optional locally (defaults to "jhonglee")
# After editing, recreate the backend so it re-reads the env:
#   docker compose -f docker-compose.dev.yml up -d backend
ANTHROPIC_API_KEY=
POSTGRES_PASSWORD=
